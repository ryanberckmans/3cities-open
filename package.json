{
  "name": "@3cities/monorepo",
  "version": "1.0.0",
  "keywords": [
    "ethereum",
    "react",
    "workspaces",
    "yarn"
  ],
  "private": true,
  "scripts": {
    "subgraph:auth": "yarn workspace @3cities/subgraph auth",
    "subgraph:codegen": "yarn workspace @3cities/subgraph codegen",
    "subgraph:build": "yarn workspace @3cities/subgraph build",
    "subgraph:deploy": "yarn workspace @3cities/subgraph deploy",
    "react-app:build": "yarn workspace @3cities/react-app build",
    "react-app:eject": "yarn workspace @3cities/react-app eject",
    "react-app:ipfs": "yarn workspace @3cities/react-app ipfs",
    "react-app:start": "yarn workspace @3cities/react-app start",
    "react-app:test": "yarn workspace @3cities/react-app test"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
